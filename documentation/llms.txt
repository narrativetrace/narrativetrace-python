# narrativetrace-python

*(Docs and published both at 0.1.1; 42 behaviours marked unreleased.)*

> Zero-boilerplate execution tracing: your method and parameter names become the log. A uv
> workspace with a dependency-free core and thin integration packages.

Wrap an object with `trace_object(obj, context)`; call it; render `context.capture_trace()`.
The narrative is generated from names and values — no logging calls in the business code.

## Install and first trace (copy this)

```bash
uv add narrativetrace
```

<!-- snippet: examples/sixty_seconds/main_llms.py -->
```python
# main.py
from narrativetrace import (
    ContextVarNarrativeContext,
    IndentedTextRenderer,
    not_traced,
    trace_object,
)


class OrderService:
    @not_traced("customer_id")
    def place_order(self, customer_id, product_id, quantity):
        return f"ORD-{customer_id}-{product_id}-{quantity}"


context = ContextVarNarrativeContext()
service = trace_object(OrderService(), context)
service.place_order("cust-1", "prod-42", 3)

print(IndentedTextRenderer().render(context.capture_trace()))
```
<!-- /snippet -->

Run: `uv run main.py`. Expected output (duration varies by machine):

<!-- snippet: examples/sixty_seconds/build/see_a_trace_with_redaction.txt mask=duration -->
```text
OrderService.place_order(customer_id: [REDACTED], product_id: "prod-42", quantity: 3) → "ORD-cust-1-prod-42-3" — 0ms
```
<!-- /snippet -->

## Before you start

- One `LoggingTraceConsumer` per event stream, always — on PyPI `0.1.1` a second one on the same events silently corrupts `nt.depth`; each instance counts its own depth *(since 0.1.2, unreleased)* so this no longer happens, but a second instance is still needless (add `logging.Handler`s instead).
- Sending a trace to your logger: on `0.1.1`, replay `store.events()` through `LoggingTraceConsumer` by hand; one-call `export_to_logger(trace)` *(since 0.1.2, unreleased)*.
- Every package installs from PyPI today (`uv add ...`); a workspace checkout is only for contributing to this repo, never required to use it.
- `narrativetrace-pytest` artifact-writing defaults **on** *(since 0.1.2, unreleased)* — PyPI's published `0.1.1` still ships it **off**; set `NARRATIVETRACE_OUTPUT=true` explicitly until the next release.
- `narrativetrace-structlog` pulls in `structlog` itself *(since 0.1.2, unreleased)* — on PyPI's published `0.1.1` it is an optional extra; add it explicitly (`uv add structlog`) on that version.
- A composite's own `__str__`/`__repr__` is never trusted for redaction *(since 0.1.2, unreleased)* — any object carrying instance state is introspected field-by-field regardless, **except a platform-defined type** (`pathlib.Path`, `datetime`, `decimal.Decimal`, `uuid.UUID`, `fractions.Fraction`, `ipaddress.*`), which keeps its own short `str()` — decided by origin (`__module__`, never a name prefix), so a lookalike or a subclass is still walked (see [Privacy and Redaction](privacy-and-redaction.md)).
- `__nt_not_traced__` is a class attribute — a tuple/list/set of field names — for redacting fields on any object a dataclass field can't reach (see the [Decorators guide](guides/decorators.md)).
- `narrativetrace-pytest` now also writes a value-free `.nt` structural artifact per test invocation, with a `manifest.json` index and a last-green delta on the suite footer *(since 0.1.2, unreleased)*; `NARRATIVETRACE_APPROVAL=true` turns on approval mode against a committed `.approved.nt` trace, promoted with `uv run poe approve` / `narrativetrace-approve` (see [Structural Trace Format](structural-trace-format.md)).

## Field notes from AI agents (self-reported, not a benchmark)
We asked AI agents to build a claims service with NarrativeTrace and write up the experience for another engineer, naming what was awkward. Prompted, one run each, unedited, model named; we wrote the library. The reports so far are from the Java runtime; the same exercise runs for Python next, and the principle they describe — the trace as evidence, redaction by parameter name, output on by default — is the same here. All six reports, the exact prompt and the task: https://narrativetrace.ai/#agents-saying
- "…the release call is present, generated from a real run, not asserted from inside the process being tested." — Claude Sonnet, 2026-09-11
- "The always-on name-based deny-list is a genuinely nice piece of design: naming a parameter `paymentToken` gets you redaction for free, and I could prove it in a test." — Claude Sonnet, 2026-09-11
- "The trace of a failed payment made missing compensation immediately visible, and approval testing caught an extra call that ordinary value assertions accepted." — GPT-6, 2026-09-08

## Packages

- [narrativetrace](../packages/narrativetrace/README.md): core — capture context, `trace_object`, `@narrated`/`@on_error`/`@not_traced`, renderers (Markdown/Prose/Indented), redaction, fork-join/fire-and-forget concurrency, canonical JSON export, stdlib logging bridge.
- [narrativetrace-pytest](../packages/narrativetrace-pytest/README.md): pytest plugin — `narrative_trace` fixture, per-test artifacts, failure narratives, suite footer with clarity split.
- [narrativetrace-diagrams](../packages/narrativetrace-diagrams/README.md): Mermaid and PlantUML sequence-diagram renderers.
- [narrativetrace-otel](../packages/narrativetrace-otel/README.md): OpenTelemetry bridge — live `OtelTraceEventListener` and batch `TraceSpanExporter` with typed `narrative.*` attributes.
- [narrativetrace-asgi](../packages/narrativetrace-asgi/README.md): ASGI middleware for Starlette/FastAPI — fail-safe request-boundary capture, W3C traceparent adopt/inject, `get_narrative_context()`.
- [narrativetrace-clarity](../packages/narrativetrace-clarity/README.md): naming-clarity engine and CI gate — 1053 verbs, abbreviation/collocation/role dictionaries, five weighted scorers, `narrativetrace-clarity` console script.
- [narrativetrace-structlog](../packages/narrativetrace-structlog/README.md): structlog processor injecting the same correlation keys as the stdlib filter.
- [narrativetrace-glossary](../packages/narrativetrace-glossary/README.md): domain glossary — ubiquitous-language harvesting, trace translation, and the vocabulary norm the clarity gate enforces.

## Guides

- [Installation](guides/installation.md)
- [Configuration](guides/configuration.md)
- [Decorators](guides/decorators.md)
- [pytest plugin](guides/pytest.md)
- [FastAPI / ASGI](guides/fastapi-asgi.md)
- [Logging & structlog](guides/logging.md)
- [OpenTelemetry](guides/opentelemetry.md)
- [Clarity](guides/clarity.md)

## Examples

- [ecommerce](../examples/ecommerce): fork-join + fire-and-forget concurrency.
- [minecraft](../examples/minecraft): generic vs domain naming, identical structure.
- [hotel_booking](../examples/hotel_booking): a generic-verb class flagged HIGH by clarity.
- [fastapi_service](../examples/fastapi_service): ASGI request traced end-to-end.
