<!-- source: README.md blob 9e7e9aa7cd18 | translated: 2026-09-09 | reviewed: - -->

# NarrativeTrace (Python)

[English](README.md) | [Español](LEAME.md) | [Português](LEIAME.md) | **简体中文**

> 代码即日志。

NarrativeTrace 把运行中的 Python 代码变成一段可读的执行叙事,这段叙事完全来自你已经写好的方法名、
类名和参数名。不需要 `logger.info(...)` 这样的语句。如果追踪读起来不清晰,说明你的代码需要更好
的命名——而不是更多的日志语句。

赶时间的话:[本地试用](#本地试用) → [接入一个测试](#接入一个测试) →
[选择你的集成方式](#选择你的集成方式)。

## 问题所在

这个方法有一半都是日志噪音:

```python
def place_order(self, customer_id, product_id, quantity):
    logger.info("Placing order for customer %s product %s qty %s", customer_id, product_id, quantity)
    inventory = self.inventory.reserve(product_id, quantity)
    logger.debug("Reserved inventory: %s", inventory)
    payment = self.payments.charge(customer_id, inventory.total)
    logger.info("Payment processed: %s", payment.transaction_id)
    return OrderResult(payment.transaction_id, inventory.items)
```

业务逻辑只有三行;日志却有四行。每个开发者写日志的方式都不一样——消息不同、级别不同、包含的
值也不同。结果就是不一致、啰嗦,并且与它描述的代码纠缠在一起。

NarrativeTrace 把这一切彻底去掉:

```python
def place_order(self, customer_id, product_id, quantity):
    inventory = self.inventory.reserve(product_id, quantity)
    payment = self.payments.charge(customer_id, inventory.total)
    return OrderResult(payment.transaction_id, inventory.items)
```

纯粹的业务逻辑。追踪是从方法名、参数名和返回值中生成的——这些信息本来就已经在那里。

## 输出是什么样子

把协作对象包装一次,然后运行代码;下面是一个三服务下单流程的真实输出(`OrderService` 调用一个
`InventoryService` 和一个 `PaymentService`,两者也都被包装了):

```
OrderService.place_order(customer_id: "cust-1", product_id: "prod-42", quantity: 3)
├── InventoryService.reserve(product_id: "prod-42", quantity: 3) → "reserved 3 of prod-42" — 0ms
├── PaymentService.charge(customer_id: "cust-1", amount: 30) → "txn-cust-1-30" — 0ms
└── → "order placed: txn-cust-1-30" — 0ms
```

**当出现问题时**,追踪会让 bug 变得清晰可见:

```
OrderService.place_order(customer_id: "cust-broke", product_id: "prod-7", quantity: 3)
├── InventoryService.reserve(product_id: "prod-7", quantity: 3) → "reserved 3 of prod-7" — 0ms
├── PaymentService.charge(customer_id: "cust-broke", amount: 30) !! PaymentDeclinedError: payment declined for customer cust-broke — 0ms
└── !! PaymentDeclinedError: payment declined for customer cust-broke — 1ms
```

`InventoryService.reserve` 被调用了,但 `InventoryService.release` 在追踪里根本找不到——不用
调试器就能看出这个 bug。

**追踪的质量完全取决于你的命名。** 同一个来自 [`examples/minecraft`](examples/minecraft) 的
“玩家加入世界”流程,被追踪了两次——一次使用领域命名,一次使用泛化命名,两次都是真实运行:

```
→ WorldServer.player_joined(player_name: "Steve")
  → WorldGenerator.generate_chunk(x: 0, z: 0)
  ← WorldGenerator.generate_chunk → Chunk(x=0, z=0, biome="plains")
  → PlayerInventory.add_item(item: Item.OAK_LOG, quantity: 4)
  ← PlayerInventory.add_item → True
  → CraftingTable.craft(recipe: Recipe.WOODEN_PICKAXE)
  ← CraftingTable.craft → Item.WOODEN_PICKAXE
  → CreatureSpawner.spawn_hostile(type: CreatureType.ZOMBIE, x: 10, y: 64, z: 20)
  ← CreatureSpawner.spawn_hostile → Creature(type=CreatureType.ZOMBIE, x=10, y=64, z=20)
← WorldServer.player_joined → "Steve joined the world"
```

```
→ GameManager.handle(input: "Steve")
  → DataProcessor.process(a: 0, b: 0)
  ← DataProcessor.process → DataResult(a=0, b=0, tag="plains")
  → StateManager.update(type: 1, count: 4)
  ← StateManager.update → True
  → ThingFactory.create(type: 1)
  ← ThingFactory.create → 1
  → EntityHandler.execute(kind: 1, a: 10, b: 64, c: 20)
  ← EntityHandler.execute → Entity(kind=1, a=10, b=64, c=20)
← GameManager.handle → "Steve joined the world"
```

同样的调用图,同样的返回值,只有命名不同——`narrativetrace-clarity` 会把这种差异量化成一个数字
(上面两次运行分别是 0.72 和 0.53)。如果你的代码讲不清楚自己的故事,那它就需要重构,这也是为
什么 NarrativeTrace 还会[为你的命名打分](documentation/zh-CN/清晰度指南.md)。

### 为什么这对 AI 辅助开发很重要

每一行 `logger.info(...)` 都是 AI 编程工具必须解析、消耗 token、并绕过去推理的一行。去掉它们,
同样的 token 预算就能覆盖更多你真正的代码,模型看到的是代码*做了什么*,而不是它*怎么记日志*,
pull request 里展示的也是业务逻辑的变更,而不是逻辑变更和日志变更混在一起。

### 与其他方案的比较

不同于面向机器和仪表盘设计的 OpenTelemetry span 树,NarrativeTrace 读起来就像面向人类和 LLM 的
散文。两者可以组合使用:`narrativetrace-otel` 会把同一条追踪以带类型的 OTel span 形式发出,因此
你能从一次捕获中同时获得人类可读的叙事*和*后端关联能力。

### 它不会取代你的日志技术栈

NarrativeTrace 不是一个日志框架。它不提供任何 handler、formatter 或投递管道——你现有的
`logging` 配置(handler、formatter、`dictConfig`/`fileConfig`)或 structlog 处理器链,会照常
运行,一切不变。

它取代的是手写的叙事*语句*——也就是上文[问题所在](#问题所在)里那些
`logger.info("Placing order %s for customer %s", ...)` 这样的行。被追踪的方法会自动产出同样的
叙事,取自调用时真实的参数值和返回值,方法体里不需要写任何叙事代码。这份叙事走的是它自己的路径
——不会拦截或重新配置你的日志管道。

有两座可选的“桥”,能让这份叙事——或者仅仅是它的身份标识——原样搭乘你现有的技术栈:

- **`LoggingTraceConsumer`** 会把生成的进入/返回/异常行,通过
  `logging.getLogger("narrativetrace").log(...)` 发出——这与手写语句会用的调用完全相同,所以
  你已有的每个 handler 和 formatter 都会照常收到它们,不受影响。
- **`narrativetrace-structlog`** 根本不生成任何行:它的处理器只是把相同的关联键
  (`traceId`、`spanId`、`nt.class`、`nt.method`……)盖到已经流经你处理器链的每个事件字典上,
  包括你自己手写的那些。`NarrativeContextFilter` 对标准库 `logging` 做的是同样的事,作为你自己
  handler 上的一个 `Filter`。

手写的 `logger.info(...)` 和被追踪的调用可以自由混用——同一个 logger、同一个流、同一批
handler。NarrativeTrace 只是往已有的东西上面添加内容。

## 本地试用

不需要项目,不需要接线——从这个仓库的一个克隆开始,直接实时运行旗舰示例:

```bash
./demo.sh --list                                   # ecommerce、hotel_booking、minecraft、library
./demo.sh --example ecommerce --no-pause           # 旗舰示例:fork-join、脱敏、失败场景
./demo.sh --example ecommerce --classic            # 同一次运行,以普通带时间戳的日志形式展示
./demo.sh --example ecommerce --lang es            # 同一条追踪,以西班牙语叙述(也支持 zh-CN)
```

`./demo.sh` 在首次运行时安装工作区(`uv sync --all-packages`,锁定的 lockfile),此后每次运行都静默
完成——不再需要单独的配置步骤。

不加 `--no-pause` 的话,demo 会在每个场景后暂停——`[Enter]` 继续,`q` 退出——每个场景开始时
都会附带一条说明,介绍*那个*追踪是如何搭建起来的。真实输出的一个片段:

```
→ OrderService.place_order(customer_id: "C-1234", product_id: "SKU-MECHANICAL-KB", quantity: 2)
  → CustomerService.find_customer(customer_id: "C-1234")
  ← CustomerService.find_customer → Customer(id="C-1234", name="Alice Johnson", tier=CustomerTier.GOLD)
⑂ fork group created [groupId: fork-1]
→ DiscountService.calculate_discount(customer_id: "C-1234", product_id: "SKU-MECHANICAL-KB")
← DiscountService.calculate_discount → Discount(percent=10)
⑃ fork joined [groupId: fork-1, members: 2]
  → PaymentService.charge(customer_id: "C-1234", amount: 166.97, card_token: [REDACTED])
  ← PaymentService.charge → PaymentConfirmation(transaction_id="TXN-00001", amount=166.97)
```

在这个仓库之外,同样的用法完全不需要任何项目——安装并运行即可:

```bash
uv add narrativetrace
```

PyPI 发布尚未完成 —— 在这些包上架索引之前,请基于本仓库的检出目录工作
(`uv sync --all-packages`)。

PyPI 发布尚未完成 —— 在这些包上架索引之前,请基于本仓库的检出目录工作
(`uv sync --all-packages`)。

```python
from narrativetrace import ContextVarNarrativeContext, MarkdownRenderer, trace_object

context = ContextVarNarrativeContext()
service = trace_object(OrderService(), context)
service.place_order("cust-1", "prod-42", 3)

print(MarkdownRenderer().render(context.capture_trace()))
```

四个 demo 示例各自讲解了什么,见 [examples/README.md](examples/README.md);ASGI 那部分示例见
[`fastapi_service`](examples/fastapi_service)(它不在 demo 启动器里——需要一个正在运行的服务器)。

## 接入一个测试

从“这个库看起来有点意思”到“我在自己的代码上看到了一条有用的追踪”,最短的路径就是 pytest 插件。
Python ≥ 3.12。

**1. 安装它**——会带入核心包、图表渲染器和清晰度引擎:

```bash
uv add --dev narrativetrace-pytest
```

**2. 在一个测试里追踪一个服务**——插件会自己注册;只需要请求这个 fixture:

```python
from narrativetrace import trace_object

class TestOrderService:
    def test_customer_places_order(self, narrative_trace):
        service = trace_object(OrderService(), narrative_trace)
        service.place_order("C-1234", "SKU-KB", 2)
```

**3. 打开产物输出并运行套件:**

```bash
NARRATIVETRACE_OUTPUT=1 uv run pytest
```

**4. 打开这份叙事**——测试类变成了目录,测试方法变成了文件:

```text
narrative-traces/traces/TestOrderService/test_customer_places_order.md
```

这是那个测试真实运行后产生的真实输出:

```markdown
---
type: trace
scenario: Test customer places order
entry_point: OrderService.place_order
duration_ms: 0
trace_id: aa4ae2eaa56e7e49b7aa42aece5999f0
trace_name: muted stone tests
method_count: 1
error_count: 0
---

## Trace: OrderService.place_order

**Scenario:** Test customer places order
**Duration:** 0ms | **Result:** PASSED

### Call Flow

- **OrderService.place_order**(customer_id: `"C-1234"`, product_id: `"SKU-KB"`, quantity: `2`) → `"ORD-C-1234-SKU-KB-2"` — 0ms
```

每个测试都会写出自己的一套产物——默认 `markdown` 格式配套的 `.json` 和
`diagrams/<Class>/<slug>.mmd`,再加上一份套件级别的 `clarity-report.md`:

```text
narrative-traces/
├── traces/<TestClass>/<slug>.md         人类可读的叙事
├── traces/<TestClass>/<slug>.json       同一条追踪的规范 JSON 形式
├── diagrams/<TestClass>/<slug>.mmd      Mermaid 时序图
├── clarity-results.json                 命名分数,每个测试一条
└── clarity-report.md                    整个套件的命名反馈
```

想继续深入——重命名方法看清晰度分数下降,加上 `@not_traced` 看一个值被脱敏?
→ [前 10 分钟](documentation/zh-CN/前10分钟.md)会用真实输出把这两件事都走一遍。

## 选择你的集成方式

大多数人从测试开始。接下来可以去这里:

| 你想要 | 从这里开始 |
|---|---|
| 在测试里获得追踪,接线最少 | `narrativetrace-pytest`(`narrative_trace` fixture) |
| 在纯 Python 中显式控制被包装的对象 | `trace_object(obj, context)`——核心包,不需要框架 |
| FastAPI/Starlette 的请求生命周期 | `narrativetrace-asgi` |
| 把追踪传播到下游的 HTTP 调用 | `narrativetrace-asgi` 的 `attach_traceparent`(httpx) |
| 在生产日志流中获得追踪 | `LoggingTraceConsumer` / `NarrativeContextFilter`(核心包) |
| structlog 流水线 | `narrativetrace-structlog` |
| OpenTelemetry span | `narrativetrace-otel` |
| 时序图 | `narrativetrace-diagrams` |
| 命名质量的 CI 门禁 | `narrativetrace-clarity` 控制台脚本 |
| Flask(WSGI)或 Django | 尚未发布——见[选择集成方式](documentation/zh-CN/选择集成方式.md) |

这里没有零代码、字节码级别的接入机制——上面的每一条路径都是一次显式的 `trace_object(...)`
调用,或者你自己注册的中间件;没有任何东西会在类加载时改写它们。完整细节、一张决策图,以及
NarrativeTrace 与其他 wrapper 叠加时会发生什么:[选择集成方式](documentation/zh-CN/选择集成方式.md)。

### 软件包

| 软件包 | 用途 |
|---|---|
| [`narrativetrace`](packages/narrativetrace) | 无依赖核心包:捕获、装饰器、渲染、脱敏、并发、JSON 导出、日志桥接 |
| [`narrativetrace-pytest`](packages/narrativetrace-pytest) | pytest 插件:逐测试的 `narrative_trace` fixture、产物、清晰度页脚 |
| [`narrativetrace-diagrams`](packages/narrativetrace-diagrams) | Mermaid + PlantUML 时序图渲染器 |
| [`narrativetrace-otel`](packages/narrativetrace-otel) | OpenTelemetry span 桥接(实时监听器 + 批量导出器) |
| [`narrativetrace-asgi`](packages/narrativetrace-asgi) | ASGI 中间件(Starlette/FastAPI)、W3C traceparent、请求访问器 |
| [`narrativetrace-clarity`](packages/narrativetrace-clarity) | 命名清晰度引擎 + CI 门禁控制台脚本 |
| [`narrativetrace-structlog`](packages/narrativetrace-structlog) | 注入关联键的 structlog 处理器 |
| [`narrativetrace-glossary`](packages/narrativetrace-glossary) | 领域术语表:`glossary.json` 模型、确定性读写器、Markdown 视图 |

## 隐私与安全

这个库运行在你的进程内部,并会写出你的团队将会分享的文件。用一张图说明这意味着什么:

| 接触面 | 能否关闭内置的脱敏? |
|---|---|
| pytest 插件、ASGI 中间件、OTel 桥接、structlog 处理器、标准库 logging 桥接 | 不能 |
| 你自己代码构造的自定义 `ValueRenderer` | 能——只能通过显式传入 `RedactionPolicy.DISABLED` |
| `@not_traced` / `not_traced_field(...)` | 不适用——它本身就是执行脱敏的机制,并且始终优先 |

在参数或字段上使用 `@not_traced`,加上基于名称的拒绝列表(`password`、`token`、`cvv`、`ssn` 等)
以及基于值形状的掩码(形似 JWT/PAN/`Set-Cookie` 的字符串,无论字段名是什么),会在任何值被反射
渲染的地方生效——没有任何随附发布的集成暴露出绕过它们的方式。一处已知且已记录的局限:一个
`{param.path}` 叙述模板始终检查默认拒绝列表,即使周围的渲染器是用自定义或已禁用的策略构建
的——精确范围见[隐私与脱敏](documentation/zh-CN/隐私与脱敏.md)。

- **追踪失败不会导致你的应用失败。** 记录在每一条路径上都做了异常隔离;一个抛出异常的
  `__str__` 或一个已满的缓冲区,永远不会改变你的方法返回或抛出的内容。
- **资源使用是有边界的。** 缓冲分析路径是一个固定大小的环(默认 65,536 个事件),在高负载下
  会丢弃而不是阻塞——并且会说明这一点:一次丢失了事件的运行会在自己的套件页脚打印丢失数量,
  而不是悄悄少报。
- **内省读取的是已存储的数据,而不是代码。** 一个计算型的 `@property` getter 永远不会执行;
  NarrativeTrace 唯一会调用的成员是一个自定义的 `__str__`、一个 `@narrative_summary` 方法,
  以及在 `@narrated`/`@on_error` 模板中被引用的属性路径——请像对待调试器一样保持它们纯净。

→ 经过对照代码验证的逐行契约见[隐私与脱敏](documentation/zh-CN/隐私与脱敏.md)。

**与其他 wrapper 共存。** 契约库、DI/AOP 代理,以及可观测性 agent(包括 OpenTelemetry 的
自动插桩)都可能包装 NarrativeTrace 所包装的同一个方法。这个实现 为自己的机制保持两条规则:
每次跨越业务边界只产生一个追踪帧——桥接方法和容器生成的机制从不会被叙述——并且没有任何结果
依赖于哪个 wrapper 在最外层,因此一次被包装调用的结果或异常,无论叠加顺序如何,始终会到达
追踪中。`narrativetrace-asgi` 的 `excluded_paths` 是今天唯一提供的排除开关(按路径精确匹配);
`trace_object` 没有按模式排除的机制,因为它包装的是你交给它的单个实例,而不是一次扫描。

## 性能

捕获由一个追踪级别控制,该级别在任何渲染发生*之前*就会被检查(`NARRATIVETRACE_LEVEL`):把它
设为 `OFF`,wrapper 就会短路——在触碰你的参数之前,没有反射,没有字符串工作。对于热点循环,
应该缩小被追踪的范围或降低级别,而不是追踪一切。

现在已经有一套起步的基准测试(`packages/narrativetrace/tests/test_bench_*.py`,用 `uv run poe
bench` 运行):每个追踪级别的捕获开销、通过 `trace_object` 的调用相对于直接调用的开销、把一次
捕获的 trace 渲染成每种格式的开销,以及热路径上脱敏检查的开销。我们还没有像 Java 姊妹项目发布
JMH 数字那样,从中发布正式数字——`uv run poe bench-gate` 会把一次运行与同一台机器上的上一次
运行做比较(一台机器的基准数字在机器之间并不可比),所以这是一个夜间习惯,而不是一个公开数字。
不要假设 Java 的数字可以直接套用:运行时不同,每一行追踪代码在其上的开销也不同。无论哪种情况,
我们都不会宣称“零开销”——追踪确实要做工作,而工作总会消耗一些成本。

## 什么是免费的,什么是 Pro

**免费**指的是这个仓库里的一切——在 BSL 1.1 下源代码可见,可免费用于生产环境,在每个版本发布
四年后转为 Apache 2.0:整个运行时、所有格式的逐测试追踪、清晰度评分、领域术语表,以及上表中的
每一项集成。

**Pro** 是构建在同一套捕获能力之上、跨越多次运行的智能能力:今天体现为事件流聚合
(`EventAggregator`——聚合树、热点、错误率),以单独的商业发行版发布,从不在这个仓库里。流程摘要、迁移 diff、运行时依赖图,以及面向 AI agent 的 MCP 工具
处理器是接下来在那边规划的内容。[功能指南](documentation/zh-CN/功能指南.md)是权威的状态表:
它把每个功能标注为免费、Pro、开发中,或已规划,并引用了每一行已发布功能背后的代码。

## 并发

fork-join 和 fire-and-forget 组会跨 `asyncio` task 和线程池传播完整的追踪身份,因此并发工作会
以一个共享的组 id 被嫁接回发起它的那个 span 之下——即使执行不是顺序的,故事依然保持连贯。一个
上下文快照会把追踪带*进*一个 worker,而在那里被追踪的工作也会*传回来*:捕获栈从它被发布的那一
刻起就会报告它,并跨越一整串 async 跳转链条传播。

## 开发

```bash
uv sync --all-packages
uv run poe check          # format-check + lint + typecheck + lint-imports + coverage + stress-quick + metrics + clarity + no-license-headers + translation-check + legal-check + bandit
```

## 文档

从这里开始:

- [前 10 分钟](documentation/zh-CN/前10分钟.md) —— 一个微型服务,真实输出,从安装到一个被脱敏的值
- [选择集成方式](documentation/zh-CN/选择集成方式.md) —— 你需要哪个包,以决策图的形式呈现
- [安装指南](documentation/zh-CN/安装指南.md) —— 每个包,各自增加了什么
- [配置指南](documentation/zh-CN/配置指南.md) —— 追踪级别、输出设置、优先级链
- [装饰器指南](documentation/zh-CN/装饰器指南.md) —— `@narrated`、`@on_error`、`@not_traced`,以及纯净性契约

深入了解:

- [隐私与脱敏](documentation/zh-CN/隐私与脱敏.md) —— 经过对照代码验证的逐行脱敏契约
- [应提交的内容](documentation/zh-CN/应提交的内容.md) —— 哪些生成的文件是 CI 产物,哪些(如果有的话)是经过评审的基线
- [故障排查](documentation/zh-CN/故障排查.md) —— 针对人们实际会遇到的失败模式,给出症状 → 原因 → 解决方法
- [pytest 指南](documentation/zh-CN/pytest插件指南.md) · [FastAPI/ASGI 指南](documentation/zh-CN/FastAPI与ASGI指南.md) · [OpenTelemetry 指南](documentation/zh-CN/OpenTelemetry指南.md) · [日志指南](documentation/zh-CN/日志与structlog指南.md) · [清晰度指南](documentation/zh-CN/清晰度指南.md)
- [功能指南](documentation/zh-CN/功能指南.md) —— 这个实现 发布的每一个功能,附带层级和状态
- [完整参考(`llms-full.md`)](documentation/llms-full.md) —— 所有指南合并为一个文件;[`llms.txt`](documentation/llms.txt) 是面向 AI agent 的机器可读索引

## 示例与 demo

可运行、经过测试的教程位于 [`examples/`](examples)——完整地图见
[`examples/README.md`](examples/README.md):`ecommerce`(旗舰示例:装饰器、失败场景、基于线程池
的 fork-join 和 fire-and-forget)、`hotel_booking`(不同命名层级下的清晰度评分)、`minecraft`
(重构前后并排对比)、`library`(会自述其身的 dataclass),以及 `fastapi_service`(ASGI 部分)。

## 许可

NarrativeTrace 的 API 和输出格式是开放标准(Apache 2.0)。它的运行时是免费且源代码可见的
(BSL 1.1,在每个版本发布四年后转为 Apache 2.0)。Pro 是商业化的。

这个仓库中的发布内容就是这个运行时:**Business Source License 1.1**(SPDX `BUSL-1.1`)——完整
文本见 [`LICENSE`](LICENSE)。生产环境使用被授权用于任何目的,包括内部使用以及你向自己客户
提供的服务;唯一的例外是将 NarrativeTrace 本身——或任何价值主要源自它的产品或服务——作为
日志、追踪或代码叙事类产品或服务提供给第三方。一个版本发布四年后,该版本会转为 Apache
License 2.0。

文档正文采用 CC BY 4.0 许可(https://creativecommons.org/licenses/by/4.0/);JSON schema 文件
采用 Apache 2.0 许可。

这些许可均不授予任何商标权;NarrativeTrace 是 Empower Agile 的商标。

### 用大白话说的许可证

这个仓库中所有可安装的内容如今都是 Business Source License 1.1;JSON schema 文件是
Apache 2.0(见 schema/README.md)。

<!-- legal:plain-words:begin -->
**免费运行。** 运行时依据 Business Source License 1.1 源码可得:你可以阅读它、审计它、修改它,
并可免费用于生产环境——包括用在你提供给自己客户的产品与服务中。

**唯一的排除项。** 你不得将 NarrativeTrace 本身——或价值实质上源自它的产品或服务——作为日志、
追踪或代码叙事产品或服务提供给第三方。

**它会在某个日期开放。** 每个发布版本都会在发布四年后转为 Apache 2.0;
确切日期印在该版本的 LICENSE 中。

*这份摘要只是一种礼貌说明,不是许可证本身。LICENSE 文件才是唯一具有约束力的文本;
若二者不一致,以 LICENSE 为准。*
<!-- legal:plain-words:end -->
